<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LibGuides Review</title>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Owner</th>
                <th>Keywords</th>
                <th># Pages</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% summary.forEach(function(document) { %>
                <tr>
                    <td><a href="<%= document.url %>" target="_blank"><%= document.name %></a></td>
                    <td><%= document.owner_name %></td>
                    <td><%= JSON.stringify(document.terms) %></td>
                    <td><%= document.page_urls.length %></td>
                    <td><a href="/inspect/<%= document.id %>">Inspect</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>